from . import ClientComponent

# All Level-specific fixes have been replaced with logic that accounts for what was being fixed, so this ClientComponent
# is currently empty.

class LevelSpecificFixes(ClientComponent):
    """
    Various level-specific fixes.
    """
